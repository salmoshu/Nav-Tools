# v1.5.0 终端 GUI 化 —— 文档集

> 目标（用户原话）：「我只是希望终端更加富文本，类似 Kimi Web 一样。」
> 形态澄清（2026-08-30）：「我执行了 ls，下面会呈现一个文件树，我可以折叠和展开文件夹……
> 我 cat 一个 markdown 内容，我也能看到 markdown 格式的文件。」
>
> 📁 本目录位于 `doc/terminal-component/`，**已在 git 中跟踪**。
> 目录与文件名使用英文，正文仍为中文（调研底稿含大量中文语境的引用与结论）。
> 面向用户的手册见 `../USER-MANUAL.md`。

## 状态快照（2026-08-30）

| 项 | 状态 |
|---|---|
| v1.5.0 终端 GUI 块视图 | ✅ 已实现（P1/P2/P3 + 打磨），本地 10 提交，**未推送** |
| 命令感知渲染（ls→文件树 / cat→Markdown） | 📋 设计分析完成，**未开工** |
| 终端演进路线图（对齐 Warp 形态、排除 AI） | ✅ 分析完成（`05-roadmap.md`），**未开工** |
| 外部调研 | ✅ 四轮完成，结论收敛：**没有可嵌入的块式终端库，自建是唯一路径** |
| 当前阶段 | **分析期**。用户明确「先分析，暂不写代码」 |

## 文档地图与阅读路径

```text
doc/terminal-component/
├── README.md                 ← 本文：索引、状态、决策快照
├── 01-requirements.md        ← 【先读】我们要什么、可行性边界、待拍板问题
├── 02-research.md            ← 【再读】外部生态横向结论（四轮调研的收敛）
├── 03-implementation.md      ← v1.5.0 已实现部分（架构、文件、不变量、已知问题）
├── 04-design.md              ← 命令感知渲染的目标设计（ls→文件树 / cat→Markdown）
├── 05-roadmap.md             ← 演进路线图（对齐 Warp 形态、排除 AI；分批计划）
└── research/                 ← 调研底稿（含 path:line 引用，按需查）
    ├── research-notes.md     第一轮：路线选择、协议与产品盘点
    ├── wave-notes.md         第二轮：Wave Terminal 源码级对比
    ├── cmux-notes.md         第三轮：cmux 对比（注意力/通知、自定义侧栏）
    └── survey-notes.md       第四轮：Warp 开源 / libghostty-vt / Zellij / 协议清单
```

| 你是谁 | 读什么 |
|---|---|
| 想快速了解全貌 | 本文 → `01-requirements.md` §2（目标形态）+ §4（可行性） |
| 要做技术决策 | `01` §4 可行性 → `02` §3 四条收敛结论 → `01` §5 待拍板 |
| 要动代码 | `03` §0 实现速览（**尤其 §0.3 三条不变量**）→ `04` §8 实施顺序 |
| 想看某个外部项目的细节 | `research/` 对应底稿（均有 `path:line` 引用） |

## 三条最重要的结论

1. **「所有命令都做 DOM 呈现」不可行。** 交互式程序（vim/less/top/ssh/sudo/进度条）
   在原理上没有 DOM 形态；命令长尾无界。Warp / Wave / cmux / VS Code 四家独立收敛到
   「真终端兜底 + 少量富化」。可行的是**分层覆盖**：L0 真终端 → L1 内容嗅探 →
   L2 高频白名单 → L3 可配置扩展。（`01` §4）

2. **不要为 GUI 重造终端模拟器。** Wave 后端把 PTY 裸字节直接落盘、零解析；
   cmux 的 OSC 解析交给 Ghostty 回调。我们现有的 `normalizeTerminalLayout`
   （约 160 行，三进三出）应删除，改为从 `xterm.buffer.active` 派生块内容。（`02` §3.1）

3. **「终端不友好」的解法可能不在终端里。** Wave 的 widgets 与 cmux 的自定义命令
   /自定义侧栏，两个独立项目收敛到同一答案：**给零命令入口**。
   这比继续打磨终端呈现更对症。（`02` §3.3）

## 决策记录（2026-08-30）

| 项 | 结论 |
|---|---|
| 文件/路径点击行为 | **块内预览**（复用 `TerminalRichContent.vue`）；目录就地展开为文件树 |
| 预设命令 | **L1 + L2 都做**，但分两批发布（L1 先发验证需求） |
| 终端是否拆为独立项目 | **暂不拆**；先做内部边界加固，稳定后转同仓 package（见 `05-roadmap.md` §5） |
| 是否使用 Warp 代码 | **不使用**（AGPL-3.0-only vs MIT）；只参照其产品形态，自行实现 |

## 待拍板

1. 是否接受**覆盖 `ls` / `cat` 内建命令**？（B4 嗅探已能覆盖大部分场景，包装的必要性下降）
2. 命令感知渲染**默认开还是默认关**？（建议先关）
3. **WSL 路径预览**：走 `wsl.exe` 转发，还是降级为不可预览
4. **L2 参数类型首版范围**：下拉 / 文本 / 布尔 是否够用
5. **预设命令作用域**：全局 / 按应用 / 按项目（项目级配置对团队协作更有价值）

## 工作约定

- **提交只留在本地**，不推送远程；push/PR 前必须先问。
- 用 conventional commit；仓库提交者为 `winchell-wsl`。
- 调研过程产生的临时产物留在 `tmp/`（已 gitignore，用完可清理）：
  原始捕获 `tmp/pty-*.bin`、复现脚本 `tmp/repro-*.mjs`、
  镜像的第三方源码 `tmp/cmux-mirror/`、`tmp/waveterm/`。
- 既有与本特性无关的测试失败：`nsat-perf`、`label-ocr`；e2e 全量需排除
  `tmp-gnss-stress.spec.ts`（缺依赖文件）。
