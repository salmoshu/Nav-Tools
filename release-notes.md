# Nav-Tools 更新日志

## v1.4.4

- 数据接入 NETWORK 配置改为统一两列网格布局，多行输入框左边界对齐；新增持久化的循环重连开关，连接失败或断线后按固定间隔自动重连
- 底部数据连接开关在点击后立即显示进行中反馈，失败时不再长时间无响应
- 相机视频与数据接入 NETWORK 共用 IP 和循环重连开关，相机配置仅保留流协议、端口与路径后缀
- 修复恢复终端时出现欢迎语重复、PROMPT_COMMAND 回显和 `;185R` 乱码的问题：SSH 不再注入启动命令，恢复的历史缓冲在回放前剥离终端查询/应答序列
- 终端标签的活动动画只对真实输出显示，输入回显不再触发
- 修复终端快捷键被不可见遮罩层误判拦截导致无响应的问题
- 终端重连不再弹出连接对话框，直接使用当前配置与安全存储凭据原位重连（凭据缺失时才回退对话框）

## v1.4.3

- 终端标签增加忙状态指示：有输出/刷新时状态圆点变为同尺寸的橙色残缺旋转环
- 同一标签下连接参数相同的 SSH 终端共享登录状态；其中一个重连成功后，其余断开的终端自动静默重连
- 终端自动跟踪当前工作目录（OSC 7），重启应用或重连后恢复到此前的目录（支持 WSL、Git Bash、bash 远端）
- 终端补齐 ANSI 16 色（Nord 调色板），ls、grep、git 等着色输出多彩可读
- 更新提醒不再展示版本更新日志，仅提示有新版本
- 修复点击“立即重启”更新时弹出安装向导的问题，自动更新全程静默安装（手动运行安装包仍保留安装引导）
- 修复 GitHub Actions Ubuntu 构建因 Electron 下载请求失败导致的构建中断

## v1.4.2

- 数据接入的串口端口刷新按钮增加旋转反馈，并覆盖端口扫描的完整过程
- 修复终端拆分或切回应用时偶发输出 `;33R`、`;72R` 的问题，历史缓冲回放不再向在线会话回写终端响应
- 终端改为右键复制选中文字、无选区时右键粘贴，并移除导致 `Ctrl+V` 重复粘贴的额外处理
- 终端标签支持拖拽换位；每个标签共享一个可切换的 SFTP 面板，拆分窗格不再重复创建文件面板
- 移除终端悬浮端口转发入口，端口转发继续在 SSH 连接设置中集中配置
- 断开和重连入口移动到终端内容底部，保留当前终端上下文并支持原位重连
- SSH 新连接默认使用密码认证；连接成功后可通过系统安全存储加密记住密码或私钥口令
- SSH 连接中的请求可随时中断，主进程会同步终止底层连接与通道创建
- 右下角通知增加关闭按钮，同时保留自动消失行为

## v1.4.1

- 应用选择器支持拖拽排序，拖动时相邻应用卡片会平滑让位，并持久化用户自定义顺序
- Terminal 的新建标签按钮改为终端类型菜单，可直接选择本地 Shell、WSL、SSH 或空终端
- 优化 Terminal 标签栏状态圆点与新增按钮布局；拆分操作改为独立的“向右拆分”和“向下拆分”按钮
- Terminal 快捷键迁移到设置页面并按组件分类，支持录入自定义组合键、冲突校验和恢复默认
- SFTP 文件面板支持鼠标拖拽和键盘调整宽度
- Terminal 会持久化活动标签、分割布局和终端类型；重启后自动恢复本地 Shell/WSL，SSH 保留连接信息并重新认证
- 终端断开或异常退出时在原窗格显示状态与重连入口，支持原位重建会话
- 修复 SSH 创建请求长期停留在“正在连接”、关闭会话后的延迟 resize 报错，以及 Windows 终端无法使用 Ctrl+V 粘贴的问题

## v1.4.0

- Raw Messages 移除通用文件发送，新增复用当前串口的 IAP 固件升级；内置 IGK IAP 模板，并支持高级协议配置、命名模板、JSON 导入导出、多种校验算法、三次重试、真实 ACK 进度及串口连接恢复
- 新增 Terminal 通用组件，支持 Windows 本地 PowerShell、CMD、Git Bash、WSL，以及 Linux/macOS 系统 Shell
- Terminal 支持 SSH 密码、私钥和 Agent 认证，读取常用 `~/.ssh/config` 配置，并提供主机指纹首次确认与变更拦截
- Terminal 支持标签页、向右/向下递归分屏、空终端或继承会话、布局恢复，以及组件分离/还原时保持活动会话
- 新增远程 SFTP 文件面板，支持上传、下载、新建目录、重命名、删除和拖拽上传
- 新增 SSH Local、Remote 和 SOCKS 动态端口转发，多规则可独立启停，单条规则失败不影响 SSH 主会话

## v1.3.1

- 设置页面改为整页设置
- 新增版本更新功能：自动检查更新、弹框提醒、自动下载
- Camera Video 组件新增循环重连开关
